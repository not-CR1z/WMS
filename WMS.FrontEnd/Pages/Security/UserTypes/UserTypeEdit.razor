@page "/usertype/edit/{Id:long}"

@if (model is null)
{
    <Loading />
}
else
{
    <FormWithName @ref="form" Label="Tipo de Usuario: " Model="model" OnValidSubmit="EditAsync" ReturnAction="Return" />
}
